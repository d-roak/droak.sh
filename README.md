# droak.sh

Personal website built with [wterm](https://github.com/d-roak/wterm), a terminal UI component library for the web.

## Development

```bash
pnpm install
pnpm dev
```

## Deployment

Automatically deployed to GitHub Pages via GitHub Actions on push to `main`.
